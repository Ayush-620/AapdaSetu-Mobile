name: aapdasetu_v2
description: "A new Flutter project."
publish_to: 'none'

version: 1.1.0+2

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.5.6
  permission_handler: ^11.3.0

  geolocator: ^14.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^7.1.0

  image_picker: ^1.2.1
  record: ^6.2.0
  flutter_image_compress: ^2.4.0
  path_provider: ^2.1.5
  path: ^1.9.1
  speech_to_text: ^7.3.0
  http: ^1.6.0

  just_audio: ^0.9.36
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/aapdasetu_logo.png

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/aapdasetu_logo.png"
  min_sdk_android: 21